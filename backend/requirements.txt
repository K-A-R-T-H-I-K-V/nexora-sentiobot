# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.4.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pin bcrypt < 4.1: passlib 1.7.4's backend init crashes on bcrypt 4.1+
# ("password cannot be longer than 72 bytes"), which breaks all login.
bcrypt==4.0.1
python-multipart==0.0.9

# LangChain / LangGraph
langchain==0.3.3
langchain-core==0.3.63
langchain-community==0.3.2
langchain-google-genai==2.0.1
langchain-groq==0.2.5
langchain-chroma==0.1.4
langgraph==0.2.38
google-genai

# Vector / Retrieval
# Embeddings run the all-MiniLM-L6-v2 model on ONNX Runtime (fastembed), NOT
# PyTorch. Same model, verified bit-identical retrieval (query cosine 1.0, hit@5
# 0.913), but ~1/3rd the memory (~280MB RAM) and the image drops ~2.8GB to ~1.5GB so it
# fits free scale-to-zero hosts. torch + sentence-transformers are gone on
# purpose (Increment 9).
fastembed==0.8.0
onnxruntime==1.24.3
chromadb==0.5.15
rank-bm25==0.2.2

# Database
supabase==2.9.0

# Utilities
python-dotenv==1.0.1
pandas==2.2.3

# Optional: Redis caching
redis==5.1.1
