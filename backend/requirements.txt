# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.4.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pin bcrypt < 4.1: passlib 1.7.4's backend init crashes on bcrypt 4.1+
# ("password cannot be longer than 72 bytes"), which breaks all login.
bcrypt==4.0.1
python-multipart==0.0.9

# LangChain / LangGraph
langchain==0.3.3
langchain-core==0.3.63
langchain-community==0.3.2
langchain-google-genai==2.0.1
langchain-groq==0.2.5
langchain-huggingface==0.1.0
langchain-chroma==0.1.4
langgraph==0.2.38
google-genai

# Vector / Retrieval
# CPU-only torch keeps the backend image small: the default Linux torch pulls
# ~2GB of CUDA/cudnn/nccl wheels (the image was ~13GB). The +cpu build resolves
# from the PyTorch CPU index below on both Linux and Windows.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0+cpu
chromadb==0.5.15
sentence-transformers==3.1.1
rank-bm25==0.2.2

# Database
supabase==2.9.0

# Utilities
python-dotenv==1.0.1
pandas==2.2.3

# Optional: Redis caching
redis==5.1.1
